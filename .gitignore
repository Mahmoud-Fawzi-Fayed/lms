# Dependencies
node_modules/
.pnp
.pnp.js

# Build
.next/
out/
build/
dist/

# Environment
.env
.env.docker
.env.local
.env.production
.env.*.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Uploads (user content)
uploads/
# Legacy thumbnail volume — public/thumbnails is mounted as a Docker volume
# (see docker-compose.yml) and contains runtime user uploads, not source.
# Keep the directory itself (.gitkeep) but ignore the UUID-named uploads.
public/thumbnails/thumb_*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# TypeScript
*.tsbuildinfo
next-env.d.ts
